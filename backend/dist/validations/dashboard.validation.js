const getStats = {
// No query parameters needed for dashboard stats
};
const getClientStatusCounts = {
// No query parameters needed for client status counts
};
export default {
    getStats,
    getClientStatusCounts
};
